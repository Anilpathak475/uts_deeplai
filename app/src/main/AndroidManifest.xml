<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidexamp.example">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FareActivity"></activity>
        <activity android:name=".SeasonRegister" />
        <activity android:name=".Stations" />
        <activity android:name=".Credential" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BookTicket" />
        <activity android:name=".NormalBook" />
        <activity android:name=".BookTravel" />
        <activity android:name=".Register" />
    </application>

</manifest>